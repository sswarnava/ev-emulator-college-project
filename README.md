# Virtual EV Charger Emulator

Minimal Node.js + TypeScript skeleton for a virtual EV charger emulator.

Quickstart

Install dependencies and start the dev server:

```bash
yarn install
yarn run dev
```

The dev command runs `ts-node-dev` against `src/server.ts`.
# ev-emulator-college-project
